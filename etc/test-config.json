{
    "test-dir": "tests",
    "recursive": true,
    "ignore-tests": [
        "sample",
        "template"
    ],

    "regex": {
        "class": {
            "source": "input.out",
            "multiple": false,
            "flags": [
                "MULTILINE"
            ],
            "group": 5
        },

        "session": {
            "source": "session.out",
            "multiple": true,
            "flags": [
                "MULTILINE"
            ],
            "group": 0
        }
    },

    "regex-order": [
        "class",
        "session"
    ]
}

