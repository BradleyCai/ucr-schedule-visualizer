{
    "test-dir": "tests",
    "test-dir-recursive": true,
    "ignore-tests": [
        "sample",
        "template"
    ],

    "regex": {
        "class": {
            "source": "input.out",
            "multiple": false
        },

        "session": {
            "source": "session.out",
            "multiple": true
        }
    }
}

