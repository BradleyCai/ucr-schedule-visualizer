{
    "test-dir": "tests",
    "recursive": true,
    "ignore-tests": [
        "sample",
        "template"
    ],

    "regex": {
        "class": {
            "source": "input.out",
            "multiple": false,
            "flags": [
                "MULTILINE"
            ]
        },

        "session": {
            "source": "session.out",
            "multiple": true,
            "flags": [
                "MULTILINE"
            ]
        }
    },

    "regex-order": [
        "class",
        "session"
    ]
}

